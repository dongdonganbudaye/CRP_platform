# RTK设备数据记录功能修改方案

## 功能概述
在RTK定位监控页面的"已注册设备"模块中，为每个设备添加"开始记录数据"功能按钮，实现每100ms记录设备的世界时间和ENU坐标数据，并支持以CSV格式导出。

## 修改内容

### 1. 前端UI修改 (`frontend/src/views/RtkMonitor.vue`)

#### 1.1 模板部分修改
- **位置**: 轨迹控制按钮区域（第137-154行）
- **修改内容**: 在"开始记录轨迹"按钮左侧添加"开始记录数据"按钮
- **新增元素**:
  ```vue
  <!-- 数据记录控制按钮 -->
  <el-button 
    size="small" 
    :type="device.dataRecording ? 'danger' : 'primary'"
    @click="toggleDataRecording(device.id)"
    class="data-record-btn"
  >
    {{ device.dataRecording ? '停止记录数据' : '开始记录数据' }}
  </el-button>
  ```

#### 1.2 脚本功能新增
- **数据状态管理**:
  - `dataRecordingDevices`: 记录正在进行数据记录的设备
  - `dataRecordIntervals`: 存储各设备的定时器
  - `recordedData`: 存储记录的数据

- **新增方法**:
  1. `toggleDataRecording(deviceId)` - 切换数据记录状态
  2. `startDataRecording(deviceId)` - 开始数据记录
  3. `stopDataRecording(deviceId)` - 停止数据记录
  4. `recordDeviceData(deviceId)` - 记录单次数据
  5. `showSaveDataDialog(deviceId)` - 显示保存数据对话框
  6. `exportDataToCSV(data, filename)` - 导出CSV文件

#### 1.3 对话框组件新增
添加数据保存确认对话框，包含：
- 是否保存数据的确认
- 自定义文件名输入
- 保存路径选择提示

### 2. 数据结构设计

#### 2.1 记录数据格式
```javascript
{
  timestamp: "2024-01-01T12:00:00.000Z", // 世界时间 (ISO 8601格式)
  deviceId: "RTK001",                    // 设备ID
  deviceName: "RTK设备1",                // 设备名称
  ipAddress: "192.168.1.100",           // IP地址
  deviceNumber: "001",                   // 设备编号
  deviceType: "移动站",                  // 设备类型
  e: 123.456,                           // E坐标 (东)
  n: 789.012,                           // N坐标 (北)
  u: 345.678                            // U坐标 (上)
}
```

#### 2.2 CSV导出格式
```csv
世界时间,设备ID,设备名称,IP地址,设备编号,设备类型,E坐标,N坐标,U坐标
2024-01-01T12:00:00.000Z,RTK001,RTK设备1,192.168.1.100,001,移动站,123.456,789.012,345.678
```

### 3. 功能流程

#### 3.1 开始记录流程
1. 用户点击"开始记录数据"按钮
2. 按钮状态变为"停止记录数据"
3. 启动100ms间隔的定时器
4. 每次定时器触发时记录当前设备数据

#### 3.2 停止记录流程
1. 用户点击"停止记录数据"按钮
2. 清除定时器，停止数据记录
3. 弹出确认对话框询问是否保存数据
4. 如果选择保存：
   - 显示文件名自定义输入框
   - 用户确认后触发CSV文件下载

#### 3.3 数据记录逻辑
- 仅记录已连接且有数据的设备
- 优先使用转换后的世界坐标，回退到原始ENU坐标
- 记录精确的世界时间戳（ISO 8601格式）
- 内存中临时存储，停止时一次性导出

### 4. 样式调整

#### 4.1 按钮布局
- 调整轨迹控制区域的flex布局
- 确保两个按钮合理排列
- 保持按钮尺寸一致性

#### 4.2 新增样式类
```css
.data-record-btn {
  font-size: 11px;
  padding: 4px 8px;
  height: auto;
  min-height: 24px;
  margin-right: 8px;
}
```

### 5. 错误处理

#### 5.1 数据有效性检查
- 检查设备连接状态
- 验证坐标数据是否存在
- 处理网络中断情况

#### 5.2 内存管理
- 限制单次记录数据量（如最多10000条）
- 提供清理功能避免内存泄漏
- 组件卸载时清理所有定时器

### 6. 用户交互优化

#### 6.1 状态指示
- 按钮颜色变化反映录制状态
- 显示已记录数据条数
- 提供录制时长显示

#### 6.2 操作反馈
- 开始/停止录制时的消息提示
- 导出成功/失败的状态反馈
- 数据为空时的友好提示

## 兼容性说明

1. **现有功能不受影响**: 新功能完全独立，不影响现有的轨迹记录功能
2. **数据格式向前兼容**: CSV格式标准化，易于后续处理
3. **性能考虑**: 100ms记录间隔对性能影响较小，数据暂存在内存中

## 预期文件修改列表

1. `frontend/src/views/RtkMonitor.vue` - 主要修改文件
   - 模板部分：添加数据记录按钮
   - 脚本部分：添加数据记录相关方法和状态
   - 样式部分：调整按钮布局样式

## 测试建议

1. **功能测试**:
   - 验证100ms记录间隔的准确性
   - 测试多设备同时记录
   - 验证CSV导出格式正确性

2. **边界测试**:
   - 设备断开连接时的处理
   - 长时间记录的内存使用
   - 大量数据导出的性能

3. **用户体验测试**:
   - 按钮状态切换的直观性
   - 保存对话框的易用性
   - 错误情况的友好提示

---

**请确认以上修改方案是否符合您的需求，如有调整建议请告知，确认后我将开始实施代码修改。** 