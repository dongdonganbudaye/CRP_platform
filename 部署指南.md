# 盖章统计小程序 - 部署指南

## 开发环境部署

### 第一步:安装开发工具

1. 访问[微信开发者工具下载页面](https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html)
2. 根据您的操作系统下载对应版本:
   - Windows 64位
   - Windows 32位
   - macOS
3. 安装开发者工具

### 第二步:导入项目

1. 打开微信开发者工具
2. 使用微信扫码登录
3. 点击左侧"小程序"选项
4. 点击"+"号或"导入项目"按钮
5. 填写项目信息:
   - **项目名称:** 盖章统计小程序
   - **目录:** 选择本项目文件夹 `E:\盖章统计小程序`
   - **AppID:** 选择"测试号"(无需注册即可开发)
6. 点击"导入"

### 第三步:本地测试

1. 项目导入后会自动编译
2. 在模拟器中预览效果
3. 点击"真机调试"可在手机上测试
4. 检查各项功能是否正常

**测试检查清单:**
- [ ] 首页按钮是否显示正常
- [ ] 首页按钮点击效果是否正常
- [ ] 签章登记页面跳转是否正常
- [ ] 表单输入是否正常
- [ ] 提交验证是否正常
- [ ] 数据导出登录是否正常
- [ ] 统计数据显示是否正确

## 生产环境部署

### 准备工作

1. **注册微信小程序账号**
   - 访问[微信公众平台](https://mp.weixin.qq.com/)
   - 点击"立即注册"
   - 选择"小程序"
   - 按照提示完成注册(需要邮箱、管理员身份验证等)
   - 完成主体信息登记

2. **获取AppID**
   - 登录微信公众平台
   - 进入"开发" → "开发管理" → "开发设置"
   - 复制 AppID

3. **配置服务器域名(可选)**
   - 如果使用了网络请求,需要配置合法域名
   - 在"开发" → "开发管理" → "开发设置" → "服务器域名"中配置
   - 域名必须是 HTTPS

### 第一步:配置AppID

1. 打开 `project.config.json` 文件
2. 修改 `appid` 字段:

```json
{
  "appid": "wx1234567890abcdef",  // 替换为您的真实AppID
  // ...其他配置
}
```

3. 保存文件
4. 重启微信开发者工具,重新导入项目

### 第二步:代码检查

1. **检查代码规范**
   - 在开发者工具中点击"详情" → "本地设置"
   - 勾选"不校验合法域名"(开发阶段)
   - 上线前取消勾选

2. **代码质量检查**
   - 点击顶部菜单"工具" → "代码质量"
   - 查看并修复警告和错误

3. **体验评分**
   - 点击"工具" → "性能评测"
   - 优化性能问题

### 第三步:上传代码

1. 点击开发者工具右上角"上传"按钮
2. 填写版本号和项目备注:
   - **版本号:** 1.0.0
   - **项目备注:** 初始版本,包含签章登记和数据导出功能
3. 点击"上传"
4. 上传成功后会显示二维码

### 第四步:提交审核

1. 登录[微信公众平台](https://mp.weixin.qq.com/)
2. 进入"管理" → "版本管理"
3. 在"开发版本"中找到刚刚上传的版本
4. 点击"提交审核"
5. 填写审核信息:

**审核信息示例:**

- **小程序类型:** 教育/办公
- **小程序功能:** 学生签章信息登记和统计
- **测试账号:** splzl123
- **测试密码:** SPl25924512
- **补充说明:** 
  ```
  这是一个用于记录学生盖章信息的内部管理工具。
  主要功能:
  1. 签章登记:记录学生姓名、一卡通号和资料类型
  2. 数据导出:管理员可导出记录数据
  
  测试流程:
  1. 点击"签章登记",填写测试数据提交
  2. 点击"数据导出",输入账号密码登录
  3. 查看统计数据并尝试导出
  ```

6. 提交审核(通常需要1-7个工作日)

### 第五步:发布上线

1. 审核通过后,在"版本管理"中会显示"审核通过"
2. 点击"发布"按钮
3. 确认发布
4. 发布成功,用户可以搜索到您的小程序

## 更新发布

### 发布新版本

1. 在本地修改代码
2. 充分测试
3. 修改版本号(如 1.0.0 → 1.0.1)
4. 重复"上传代码"和"提交审核"流程
5. 审核通过后发布

### 版本回退

1. 在"版本管理"中
2. 找到历史版本
3. 点击"回退"
4. 确认回退

## 数据迁移(可选)

### 从本地存储迁移到云数据库

如果用户量增大,建议使用云数据库:

#### 1. 开通云开发

1. 在微信公众平台开通云开发
2. 在开发者工具中点击"云开发"按钮
3. 创建云环境

#### 2. 创建数据库集合

创建两个集合:
- `agreement_data` - 就业协议书数据
- `registration_data` - 报到证数据

#### 3. 修改代码

替换本地存储为云数据库调用:

**原代码(本地存储):**
```javascript
const data = wx.getStorageSync('agreementData') || []
data.push(newRecord)
wx.setStorageSync('agreementData', data)
```

**新代码(云数据库):**
```javascript
const db = wx.cloud.database()
db.collection('agreement_data').add({
  data: newRecord
}).then(res => {
  console.log('添加成功', res)
})
```

#### 4. 数据导出迁移

使用云函数处理数据导出:

```javascript
// 云函数: exportData
const cloud = require('wx-server-sdk')
cloud.init()
const db = cloud.database()

exports.main = async (event, context) => {
  const { type } = event
  const collection = type === '就业协议书' ? 'agreement_data' : 'registration_data'
  
  const result = await db.collection(collection).get()
  return result.data
}
```

## 运维管理

### 监控数据

1. 在微信公众平台查看数据统计
2. 关注用户反馈
3. 定期检查错误日志

### 数据备份

**本地存储版本:**
- 提醒管理员定期导出数据
- 保存CSV文件作为备份

**云数据库版本:**
- 使用云开发控制台导出数据
- 设置自动备份

### 应急预案

**遇到问题时:**
1. 在"版本管理"中回退到稳定版本
2. 修复问题后重新提交审核
3. 必要时可以使用"紧急修复"功能

## 成本预估

### 测试阶段
- **开发者工具:** 免费
- **测试账号:** 免费
- **本地存储:** 免费

### 生产环境(小规模使用)
- **小程序注册:** 300元/年(认证费,个人类型免费但功能受限)
- **服务器(如使用):** 按需,最低约10元/月
- **云开发(如使用):** 免费额度内可满足小规模使用

### 生产环境(大规模使用)
- **小程序认证:** 300元/年
- **云开发资源包:** 根据用户量,约50-500元/月
- **CDN(如使用):** 按流量计费

## 注意事项

1. **隐私保护**
   - 填写隐私政策
   - 说明数据收集和使用方式
   - 遵守《个人信息保护法》

2. **用户协议**
   - 制定用户使用协议
   - 明确数据所有权

3. **内容审核**
   - 确保内容合法合规
   - 不包含违禁信息

4. **性能优化**
   - 代码体积不超过2MB
   - 图片资源压缩
   - 减少网络请求

5. **兼容性**
   - 测试不同机型
   - 测试不同微信版本
   - 做好降级处理

## 技术支持

### 官方资源
- [微信开放社区](https://developers.weixin.qq.com/community/)
- [小程序开发文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [云开发文档](https://developers.weixin.qq.com/miniprogram/dev/wxcloud/basis/getting-started.html)

### 常见问题
- 审核被拒:查看拒绝原因,按要求修改
- 功能异常:查看调试器错误日志
- 性能问题:使用性能分析工具

---

**祝您部署顺利!**

如有问题,请参考官方文档或联系微信开放社区。

