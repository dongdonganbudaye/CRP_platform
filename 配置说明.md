# 盖章统计小程序 - 配置说明

## 项目配置详解

### 1. 颜色配置

项目中使用的主要颜色:

```
主色调(绿色): #587558
强调色(黄色): #FFB414  
白色背景: #FFFFFF
文字颜色: #333333
次要文字: #666666 / #999999
边框颜色: #E0E0E0
背景灰色: #F8F8F8 / #FAFAFA
```

如需修改主题颜色,可以在各个页面的 `.wxss` 文件中全局替换对应的色号。

### 2. 导出账号配置

当前数据导出页面的登录信息:

- **账号:** `splzl123`
- **密码:** `SPl25924512`

**修改方法:**

编辑 `pages/export/export.js` 文件,找到以下代码:

```javascript
// 验证账号密码
if (username === 'splzl123' && password === 'SPl25924512') {
```

将账号和密码修改为您需要的值。

**安全建议:**
- 建议使用更复杂的密码
- 如果要上线使用,建议配置后端API进行验证
- 可以添加多个管理员账号

### 3. 签章资料类型配置

当前支持的资料类型:
- 就业协议书
- 报到证

**添加新的资料类型:**

编辑 `pages/register/register.js` 文件,修改 `documentTypes` 数组:

```javascript
data: {
  documentTypes: ['就业协议书', '报到证', '新增类型名称'],
  // ...
}
```

同时需要修改存储逻辑,在 `pages/register/register.js` 的 `onSubmit` 方法中:

```javascript
// 根据新的类型添加相应的存储键
const storageKey = documentType === '就业协议书' ? 'agreementData' : 
                   documentType === '报到证' ? 'registrationData' : 
                   'newTypeData'
```

### 4. AppID配置

**测试环境:**
- 当前使用测试AppID: `touristappid`
- 适用于本地开发和测试

**正式环境:**

1. 在[微信公众平台](https://mp.weixin.qq.com/)注册小程序账号
2. 获取正式的AppID
3. 编辑 `project.config.json` 文件:

```json
{
  "appid": "您的正式AppID",
  // ...
}
```

### 5. 页面导航配置

页面路由在 `app.json` 中配置:

```json
{
  "pages": [
    "pages/index/index",       // 首页
    "pages/register/register", // 签章登记
    "pages/export/export",     // 数据导出
    "pages/success/success"    // 提交成功
  ]
}
```

**添加新页面:**
1. 在 `pages/` 目录下创建新的页面文件夹
2. 在 `app.json` 的 `pages` 数组中添加页面路径

### 6. 导航栏配置

全局导航栏配置在 `app.json` 中:

```json
{
  "window": {
    "backgroundTextStyle": "light",
    "navigationBarBackgroundColor": "#587558",  // 导航栏背景色
    "navigationBarTitleText": "盖章统计小程序",   // 默认标题
    "navigationBarTextStyle": "white"           // 标题文字颜色
  }
}
```

**单独页面导航栏:**

在各页面的 `.json` 文件中可以覆盖全局配置:

```json
{
  "navigationBarTitleText": "签章登记"
}
```

### 7. 数据存储配置

当前使用微信本地存储(Storage):

**存储键名:**
- `agreementData` - 就业协议书数据
- `registrationData` - 报到证数据

**数据结构:**
```javascript
{
  name: "张三",              // 姓名
  cardNumber: "2023001",     // 一卡通号
  documentType: "就业协议书", // 资料类型
  timestamp: 1699344000000,  // 时间戳
  dateTime: "2025/11/07 14:30:00" // 格式化时间
}
```

**迁移到云数据库:**

如果未来需要使用云数据库,可以使用微信云开发:

1. 开通云开发功能
2. 创建数据库集合
3. 修改存储逻辑,将 `wx.setStorageSync` 替换为云数据库API

### 8. 导出功能配置

CSV文件导出配置:

**文件编码:** UTF-8 with BOM (确保Excel能正确显示中文)

**文件名格式:** `{数据类型}_{时间戳}.csv`

**CSV表头:** 姓名,一卡通号,资料类型,提交时间

**修改导出格式:**

编辑 `pages/export/export.js` 的 `convertToCSV` 方法:

```javascript
convertToCSV(data, type) {
  // 修改表头
  let csv = '\ufeff姓名,一卡通号,资料类型,提交时间,备注\n'
  
  // 修改数据行
  data.forEach(item => {
    csv += `${item.name},${item.cardNumber},${item.documentType},${item.dateTime},${item.remark || ''}\n`
  })

  return csv
}
```

### 9. 表单验证配置

当前验证规则:
- 姓名和一卡通号不能为空
- 同一资料类型中姓名不能重复
- 同一资料类型中一卡通号不能重复

**添加更多验证规则:**

编辑 `pages/register/register.js` 的 `onSubmit` 方法:

```javascript
// 验证一卡通号格式(示例)
if (!/^\d{7}$/.test(cardNumber.trim())) {
  wx.showModal({
    title: '提示',
    content: '一卡通号必须为7位数字',
    showCancel: false
  })
  return
}

// 验证姓名长度(示例)
if (name.trim().length < 2 || name.trim().length > 20) {
  wx.showModal({
    title: '提示',
    content: '姓名长度必须在2-20个字符之间',
    showCancel: false
  })
  return
}
```

### 10. 按钮交互配置

按钮点击效果:
- 按下时: 颜色从 `#587558` 变为 `#FFB414`
- 松开时: 恢复为 `#587558`

实现方式:
```javascript
// 使用 bindtouchstart 和 bindtouchend 事件
bindtouchstart="onButtonTouchStart"
bindtouchend="onButtonTouchEnd"
```

如需修改为长按效果:
```javascript
// 使用 bindlongpress 事件
bindlongpress="onButtonLongPress"
```

## 开发建议

### 1. 代码规范
- 使用两个空格缩进
- 变量命名使用驼峰命名法
- 函数命名清晰表达意图

### 2. 性能优化
- 避免在 data 中存储大量数据
- 及时清理不需要的监听器
- 使用分页加载大量数据

### 3. 用户体验
- 添加加载提示(wx.showLoading)
- 网络请求失败时给予友好提示
- 按钮点击后防止重复提交

### 4. 安全建议
- 不要在代码中硬编码敏感信息
- 使用HTTPS协议
- 对用户输入进行过滤和验证

## 常用命令

### 在微信开发者工具中

- **F5** - 刷新
- **Ctrl + S** - 保存并编译
- **Ctrl + Shift + M** - 切换模拟器/编辑器比例
- **Ctrl + Shift + C** - 打开调试器

## 相关文档

- [微信小程序官方文档](https://developers.weixin.qq.com/miniprogram/dev/framework/)
- [微信开发者社区](https://developers.weixin.qq.com/community/develop/mixflow)
- [小程序代码片段](https://developers.weixin.qq.com/miniprogram/dev/devtools/minicode.html)

---

**最后更新:** 2025-11-07

