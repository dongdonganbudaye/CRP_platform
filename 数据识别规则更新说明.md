# 数据识别规则更新说明 v1.2.0

## 🔄 更新日期
2025年11月7日

---

## 📋 更新内容

### 核心变更: 数据识别逻辑

#### 🔴 旧逻辑(v1.0/v1.1)
```
- 分别在"就业协议书"和"就业推荐表"两个库中检测重复
- 同一库中: 姓名不能重复,一卡通号不能重复
- 不同库之间: 允许重复(同一学生可在两个库中各提交一次)
```

#### 🟢 新逻辑(v1.2.0)
```
- 以"一卡通号"为唯一标识
- 合并两个库统一检测
- 同一个一卡通号只对应一个姓名
- 同一个一卡通号可以提交多种类型的资料
- 但每种类型只能提交一次
```

---

## 🎯 新规则详解

### 规则1: 一卡通号唯一性

**说明**: 一个一卡通号在整个系统中只对应一个学生(一个姓名)

**示例**:
```
✅ 允许:
  张三 - 2023001 - 就业协议书
  张三 - 2023001 - 就业推荐表

❌ 不允许:
  张三 - 2023001 - 就业协议书
  李四 - 2023001 - 就业推荐表  (同一一卡通号对应不同姓名)
```

### 规则2: 姓名一致性检查

**说明**: 如果一卡通号已存在,提交时会检查姓名是否一致

**流程**:
```
情况A: 首次提交
  → 直接保存

情况B: 该一卡通号已存在,姓名一致
  → 检查是否已提交过该类型资料
  → 如果已提交: 提示"已提交过该类型"
  → 如果未提交: 允许提交

情况C: 该一卡通号已存在,姓名不一致
  → 弹出提示: "一卡通号XXX已登记为'张三',您输入的是'李四'"
  → 选项1: 确认继续(使用原有姓名)
  → 选项2: 取消重新输入
```

### 规则3: 资料类型独立性

**说明**: 同一学生可以提交多种类型的资料

**示例**:
```
张三 - 2023001:
  ✅ 可以提交"就业协议书"
  ✅ 可以提交"就业推荐表"
  ❌ 不能提交两次"就业协议书"
  ❌ 不能提交两次"就业推荐表"
```

---

## 💻 技术实现

### 新增文件

#### 1. `utils/studentRecords.js`
学生记录管理工具类,提供以下功能:

```javascript
// 根据一卡通号获取学生所有记录
getStudentRecords(cardNumber)

// 检查是否已提交指定类型资料
hasSubmitted(cardNumber, documentType)

// 获取一卡通号对应的姓名
getNameByCardNumber(cardNumber)

// 获取所有学生汇总信息
getAllStudentsSummary()

// 获取统计信息
getStatistics()

// 检查姓名和一卡通号是否匹配
isNameCardMatch(name, cardNumber)
```

### 修改文件

#### 1. `pages/register/register.js`

**主要变更**:
- 合并检查两个库的数据
- 添加姓名一致性验证
- 新增 `saveRecord()` 方法

**关键代码**:
```javascript
// 获取所有数据(合并两个库)
const agreementData = wx.getStorageSync('agreementData') || []
const registrationData = wx.getStorageSync('registrationData') || []
const allData = [...agreementData, ...registrationData]

// 检查该一卡通号是否已提交过该类型的资料
const existingRecord = allData.find(item => 
  item.cardNumber === cardNumber.trim() && 
  item.documentType === documentType
)

// 检查姓名一致性
const cardRecord = allData.find(item => 
  item.cardNumber === cardNumber.trim()
)
if (cardRecord && cardRecord.name !== name.trim()) {
  // 提示姓名不一致
}
```

#### 2. `pages/export/export.js`

**主要变更**:
- 引入 `studentRecords` 工具类
- 更新统计逻辑
- 新增统计维度

**新增统计项**:
```javascript
{
  totalStudents: 50,      // 总学生数(按一卡通号去重)
  agreementCount: 45,     // 提交就业协议书的学生数
  registrationCount: 40,  // 提交就业推荐表的学生数
  bothSubmitted: 35,      // 两种都提交的学生数
  totalRecords: 85        // 总记录数
}
```

#### 3. `pages/export/export.wxml`

**界面更新**:
- 新增"总学生数"显示(高亮黄色)
- 将"记录数"改为"人数"
- 新增"两种都提交"统计

#### 4. `pages/export/export.wxss`

**样式新增**:
```css
.stat-item.highlight {
  background-color: #FFB414;  /* 黄色高亮 */
  /* 用于突出显示总学生数 */
}
```

---

## 📊 数据统计变化

### 旧统计方式
```
就业协议书记录: 50条
就业推荐表记录: 45条
总记录数: 95条
```

### 新统计方式
```
👥 总学生数: 55人          (高亮显示)
就业协议书: 50人
就业推荐表: 45人
两种都提交: 40人
总记录数: 95条
```

**说明**:
- **总学生数**: 按一卡通号去重后的人数
- **就业协议书**: 提交过就业协议书的学生数
- **就业推荐表**: 提交过就业推荐表的学生数
- **两种都提交**: 两种资料都提交的学生数
- **总记录数**: 所有提交记录的总数

---

## 🔍 使用场景示例

### 场景1: 新学生首次提交

```
输入:
  姓名: 张三
  一卡通号: 2023001
  资料类型: 就业协议书

结果: ✅ 提交成功
```

### 场景2: 同学生提交第二种资料

```
已有记录:
  张三 - 2023001 - 就业协议书

新提交:
  姓名: 张三
  一卡通号: 2023001
  资料类型: 就业推荐表

结果: ✅ 提交成功
```

### 场景3: 重复提交同类型资料

```
已有记录:
  张三 - 2023001 - 就业协议书

新提交:
  姓名: 张三
  一卡通号: 2023001
  资料类型: 就业协议书

结果: ❌ 提示"一卡通号2023001已提交过就业协议书"
```

### 场景4: 一卡通号对应姓名不一致

```
已有记录:
  张三 - 2023001 - 就业协议书

新提交:
  姓名: 李四
  一卡通号: 2023001
  资料类型: 就业推荐表

结果: ⚠️ 弹出提示框
  "一卡通号2023001已登记为'张三',
   您输入的姓名是'李四',
   是否继续提交?"
  
  点击"确定": 使用原姓名"张三"提交
  点击"取消": 返回修改
```

### 场景5: 姓名输入错误

```
已有记录:
  张三 - 2023001 - 就业协议书

新提交:
  姓名: 张山 (笔误)
  一卡通号: 2023001
  资料类型: 就业推荐表

结果: ⚠️ 系统提示姓名不一致
  管理员可选择:
  - 使用正确姓名"张三"继续提交
  - 或取消重新输入
```

---

## 🎯 优势分析

### ✅ 优点

1. **数据一致性更强**
   - 一个一卡通号只对应一个姓名
   - 避免了同一学生在不同库中姓名不一致的情况

2. **统计更准确**
   - 可以准确统计学生总数
   - 可以统计提交多种资料的学生数
   - 避免重复计数

3. **更符合实际业务**
   - 一卡通号是学生的唯一标识
   - 姓名可能有笔误,但一卡通号不会错

4. **防错机制更完善**
   - 自动检测姓名不一致
   - 给出明确提示
   - 允许管理员确认

### ⚠️ 注意事项

1. **姓名不一致处理**
   - 如果确实是不同学生,应该重新核对一卡通号
   - 系统会使用首次登记的姓名

2. **数据迁移**
   - 已有数据不受影响
   - 新规则从更新后生效

3. **导出数据**
   - 导出的CSV文件格式不变
   - 但统计方式变化

---

## 📝 测试建议

### 测试用例1: 正常提交
```
1. 提交: 张三 - 2023001 - 就业协议书
2. 提交: 张三 - 2023001 - 就业推荐表
3. 期望: 两次都成功
4. 统计: 总学生数=1, 总记录数=2
```

### 测试用例2: 重复检测
```
1. 提交: 张三 - 2023001 - 就业协议书
2. 再次提交: 张三 - 2023001 - 就业协议书
3. 期望: 第二次提示"已提交过就业协议书"
```

### 测试用例3: 姓名不一致
```
1. 提交: 张三 - 2023001 - 就业协议书
2. 提交: 李四 - 2023001 - 就业推荐表
3. 期望: 提示姓名不一致
4. 确认后: 使用"张三"保存
```

### 测试用例4: 多学生提交
```
1. 提交: 张三 - 2023001 - 就业协议书
2. 提交: 李四 - 2023002 - 就业协议书
3. 提交: 张三 - 2023001 - 就业推荐表
4. 期望: 都成功
5. 统计: 总学生数=2, 总记录数=3, 两种都提交=1
```

---

## 🔄 版本兼容性

### 数据兼容性
- ✅ 向后兼容
- ✅ 已有数据正常使用
- ✅ 不需要数据迁移

### 功能兼容性
- ✅ 导出功能正常
- ✅ 界面正常显示
- ✅ 所有原有功能保持不变

---

## 📞 常见问题

### Q1: 如果学生改名了怎么办?
A: 系统会保持首次登记的姓名。如需修改,需要管理员在导出后的数据中手动修改,或者清除该学生的所有记录重新登记。

### Q2: 统计的"总学生数"是如何计算的?
A: 按一卡通号去重。例如:
- 张三-2023001提交了2种资料(2条记录)
- 李四-2023002提交了1种资料(1条记录)
- 总学生数=2人,总记录数=3条

### Q3: "两种都提交"是什么意思?
A: 指同时提交了"就业协议书"和"就业推荐表"两种资料的学生数量。

### Q4: 如果姓名输入错误怎么办?
A: 系统会提示姓名不一致。管理员可以:
1. 如果一卡通号确实是同一学生,选择"确定"继续
2. 如果发现输入错误,选择"取消"重新输入

### Q5: 导出的数据格式有变化吗?
A: CSV文件格式不变,仍然是:姓名、一卡通号、资料类型、提交时间。但统计页面会显示更详细的信息。

---

## ✅ 更新检查清单

更新后请检查:

- [ ] 能正常提交新记录
- [ ] 重复提交会被拦截
- [ ] 姓名不一致会有提示
- [ ] 统计显示"总学生数"等新字段
- [ ] 导出功能正常
- [ ] 原有数据仍能正常查看

---

## 🎉 总结

**核心改进**: 以一卡通号为准,确保数据一致性和统计准确性

**主要优势**:
- ✅ 数据更规范
- ✅ 统计更准确
- ✅ 防错更完善
- ✅ 完全兼容旧数据

**影响范围**:
- 提交逻辑
- 统计方式
- 界面显示

**向后兼容**: 100%兼容

---

**更新版本**: v1.2.0  
**更新日期**: 2025年11月7日  
**状态**: ✅ 已完成

