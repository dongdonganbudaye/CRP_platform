# 建筑模型显示模式分层展示修改方案

## 修改概述
改进"建筑模型管理"中的"显示模式"功能，将模型按配准阶段进行分层管理：
- **设计模型**：上传的原始模型 + 阶段1（基准点配准）处理后的模型
- **实际模型**：阶段2（精确配准/Kabsch算法）处理后的模型

## 当前状态分析

### 现有显示模式功能
- **叠加模式**：显示所有模型 + 显示RTK设备
- **仅设计模式**：显示所有模型，隐藏RTK设备
- **仅实际模式**：隐藏所有模型，显示RTK设备

### 现有问题
1. 无法区分不同配准阶段的模型
2. "设计"和"实际"的概念与配准流程不匹配
3. 用户无法单独查看原始设计模型vs精确配准后的模型

## 修改方案

### 方案概念
将模型分为两个层次：
1. **设计模型层**：包含上传的原始模型和经过阶段1（基准点配准）的模型
2. **实际模型层**：包含经过阶段2（Kabsch精确配准）的模型

### 技术实现思路

#### 1. 模型状态标记
为每个模型添加配准阶段标记：

```javascript
// 建筑模型状态扩展
const buildingModels = ref([]);
// 每个模型对象将包含：
// {
//   id: 'model_id',
//   name: 'model_name', 
//   visible: true/false,
//   alignmentStage: 'original' | 'stage1' | 'stage2'
// }

// 模型配准阶段状态
const modelAlignmentStage = ref('original'); // 'original', 'stage1', 'stage2'
```

#### 2. 模型加载时的初始状态
```javascript
// 在handleModelUpload函数中
async function handleModelUpload(file) {
  // ... 现有上传逻辑 ...
  
  const metadata = {
    name: file.name.replace(/\.(gltf|glb|obj|stl)$/i, ''),
    position: { x: 0, y: 0, z: 0 },
    rotation: { x: 0, y: 0, z: 0 },
    scale: { x: 1, y: 1, z: 1 },
    opacity: modelOpacity.value,
    visible: true,
    alignmentStage: 'original' // 新增：初始为原始状态
  };
  
  // ... 其余逻辑 ...
}
```

#### 3. 配准阶段状态更新

##### 3.1 阶段1完成时更新模型状态
```javascript
// 在setBimBasePoint函数中
function setBimBasePoint(vertex) {
  // ... 现有变换逻辑 ...
  
  // 更新所有模型的配准阶段状态
  buildingModels.value.forEach(model => {
    if (model.alignmentStage === 'original') {
      model.alignmentStage = 'stage1';
    }
  });
  
  // ... 保存状态等 ...
}
```

##### 3.2 阶段2完成时创建实际模型层
```javascript
// 在performKabschAlignment函数中
async function performKabschAlignment() {
  // ... 现有Kabsch配准逻辑 ...
  
  // 创建实际模型层（虚拟概念，实际上是对现有模型应用变换）
  buildingModels.value.forEach(model => {
    if (model.alignmentStage === 'stage1') {
      model.alignmentStage = 'stage2';
    }
  });
  
  // ... 其余逻辑 ...
}
```

#### 4. 显示模式控制更新

##### 4.1 更新updateModelLayerMode函数
```javascript
function updateModelLayerMode(mode) {
  if (mode === 'design') {
    // 显示设计模型层（原始 + 阶段1）
    Object.entries(loadedModels.value).forEach(([modelId, model]) => {
      const modelInfo = buildingModels.value.find(m => m.id === modelId);
      if (modelInfo) {
        // 显示原始和阶段1的模型
        const shouldShow = modelInfo.alignmentStage === 'original' || 
                          modelInfo.alignmentStage === 'stage1';
        model.visible = shouldShow && modelInfo.visible !== false;
      }
    });
    
    // 隐藏RTK设备
    Object.values(deviceMeshes.value).forEach(mesh => {
      mesh.visible = false;
    });
    
  } else if (mode === 'actual') {
    // 显示实际模型层（阶段2）
    Object.entries(loadedModels.value).forEach(([modelId, model]) => {
      const modelInfo = buildingModels.value.find(m => m.id === modelId);
      if (modelInfo) {
        // 只显示阶段2的模型
        const shouldShow = modelInfo.alignmentStage === 'stage2';
        model.visible = shouldShow && modelInfo.visible !== false;
      }
    });
    
    // 显示RTK设备
    Object.values(deviceMeshes.value).forEach(mesh => {
      mesh.visible = true;
    });
    
  } else {
    // 叠加显示所有
    Object.entries(loadedModels.value).forEach(([modelId, model]) => {
      const modelInfo = buildingModels.value.find(m => m.id === modelId);
      if (modelInfo) {
        model.visible = modelInfo.visible !== false;
      }
    });
    
    Object.values(deviceMeshes.value).forEach(mesh => {
      mesh.visible = true;
    });
  }
}
```

##### 4.2 状态恢复时的处理
```javascript
// 在restoreAlignmentState函数中
function restoreAlignmentState() {
  // ... 现有恢复逻辑 ...
  
  // 根据配准完成状态更新模型阶段
  if (alignmentManager.value.stage2Complete) {
    buildingModels.value.forEach(model => {
      model.alignmentStage = 'stage2';
    });
  } else if (alignmentManager.value.stage1Complete) {
    buildingModels.value.forEach(model => {
      model.alignmentStage = 'stage1';
    });
  }
  
  // ... 其余逻辑 ...
}
```

### 5. UI改进

#### 5.1 显示模式按钮文字更新
```vue
<div class="control-item">
  <span class="control-label">显示模式:</span>
  <el-radio-group v-model="modelLayerMode" size="small" @change="updateModelLayerMode">
    <el-radio-button value="overlay">叠加</el-radio-button>
    <el-radio-button value="design">设计模型</el-radio-button>
    <el-radio-button value="actual">实际模型</el-radio-button>
  </el-radio-group>
</div>
```

#### 5.2 模型列表中显示配准状态
```vue
<div v-for="model in buildingModels" :key="model.id" class="model-item">
  <div class="model-info">
    <!-- 颜色指示器 -->
    <div class="device-color-indicator" 
         :style="{ backgroundColor: getDeviceColor(model.id, false).css }"
         :title="`设备颜色: ${getDeviceColor(model.id, false).name}`">
    </div>
    <span class="model-name">{{ model.name }}</span>
    <span class="model-size">{{ model.format }}</span>
    <!-- 新增：配准状态标识 -->
    <span class="alignment-stage-badge" :class="model.alignmentStage">
      {{ getAlignmentStageText(model.alignmentStage) }}
    </span>
  </div>
  <!-- ... 按钮区域 ... -->
</div>
```

#### 5.3 配准状态文字和样式
```javascript
function getAlignmentStageText(stage) {
  switch(stage) {
    case 'original': return '原始';
    case 'stage1': return '设计';
    case 'stage2': return '实际';
    default: return '未知';
  }
}
```

```css
.alignment-stage-badge {
  font-size: 10px;
  padding: 2px 6px;
  border-radius: 3px;
  margin-left: 8px;
}

.alignment-stage-badge.original {
  background: rgba(128, 128, 128, 0.3);
  color: #888;
}

.alignment-stage-badge.stage1 {
  background: rgba(52, 152, 219, 0.3);
  color: #3498db;
}

.alignment-stage-badge.stage2 {
  background: rgba(46, 204, 113, 0.3);
  color: #2ecc71;
}
```

## 用户体验流程

### 操作流程
1. **模型上传**: 模型显示为"原始"状态
2. **阶段1配准**: 完成基准点配准后，模型状态变为"设计"
3. **阶段2配准**: 完成Kabsch配准后，模型状态变为"实际"

### 显示模式使用
- **叠加模式**: 查看所有模型和RTK设备的完整场景
- **设计模型模式**: 查看原始设计意图（原始+基准点配准）
- **实际模型模式**: 查看精确配准后的实际位置

### 典型使用场景
1. **设计验证**: 切换到"设计模型"查看原始设计和初步配准
2. **精度检查**: 切换到"实际模型"查看最终配准结果与RTK数据的对比
3. **对比分析**: 在"叠加"模式下对比设计与实际的差异

## 技术优势

### 1. 清晰的概念映射
- **设计模型** = 设计意图（原始模型 + 基准点对齐）
- **实际模型** = 现实测量（精确配准后的模型）

### 2. 渐进式增强
- 不破坏现有功能
- 向后兼容现有配准状态
- 逐步引导用户理解配准流程

### 3. 视觉反馈
- 模型状态一目了然
- 配准进度可视化
- 操作结果即时反馈

## 实现风险评估

### 低风险修改
- 主要是状态管理和显示逻辑
- 不影响核心配准算法
- 可以渐进式实现

### 向后兼容
- 现有配准数据可以正常工作
- 旧的显示模式逻辑保持可用
- 状态恢复机制完整

## 修改文件列表
- `frontend/src/views/RtkMonitor.vue` (主要修改)

## 是否确认进行此修改？
请确认是否按照以上方案进行修改，将显示模式与配准阶段进行关联。如有调整需求，请告知具体要求。 