# 建筑模型单独显示隐藏功能修改方案

## 修改概述
在"建筑模型管理"栏中为每个已加载的模型添加单独的显示/隐藏切换按钮，位置在现有的灰色编辑按钮和红色删除按钮的左侧。

## 当前状态分析
- **现有位置**: `frontend/src/views/RtkMonitor.vue` 第223-250行
- **现有按钮**: 
  - 灰色编辑按钮 (type="info", 带编辑图标)
  - 红色删除按钮 (type="danger", 带删除图标)
- **现有功能**: 全局显示/隐藏开关，全局透明度控制，显示模式切换

## 修改详情

### 1. 前端界面修改 (`frontend/src/views/RtkMonitor.vue`)

#### 1.1 HTML模板修改 (第231-248行)
```vue
<div class="model-actions">
  <!-- 新增：单独显示/隐藏按钮 -->
  <el-button 
    size="small" 
    :type="model.visible !== false ? 'success' : 'warning'"
    circle
    @click="toggleModelVisibility(model.id)"
    :title="model.visible !== false ? '隐藏模型' : '显示模型'"
  >
    <i :class="model.visible !== false ? 'el-icon-view' : 'el-icon-hide'"></i>
  </el-button>
  
  <!-- 现有的编辑按钮 -->
  <el-button 
    size="small" 
    type="info"
    circle
    @click="editModel(model)"
  >
    <i class="el-icon-edit"></i>
  </el-button>
  
  <!-- 现有的删除按钮 -->
  <el-button 
    size="small" 
    type="danger"
    circle
    @click="deleteModel(model.id)"
  >
    <i class="el-icon-delete"></i>
  </el-button>
</div>
```

#### 1.2 JavaScript函数新增
在现有的建筑模型相关函数部分（约第1485行后）添加新函数：

```javascript
// 切换单个模型的显示/隐藏状态
async function toggleModelVisibility(modelId) {
  try {
    // 找到对应的模型
    const model = buildingModels.value.find(m => m.id === modelId);
    if (!model) return;
    
    // 切换可见状态
    const newVisibility = model.visible !== false ? false : true;
    model.visible = newVisibility;
    
    // 更新3D场景中的模型显示
    if (loadedModels.value[modelId]) {
      loadedModels.value[modelId].visible = newVisibility;
    }
    
    // 同步到后端
    const response = await axios.put(`${apiBaseUrl}/api/models/${modelId}`, {
      ...model,
      visible: newVisibility
    });
    
    if (response.data.success) {
      ElMessage.success(newVisibility ? '模型已显示' : '模型已隐藏');
    } else {
      ElMessage.error('更新模型状态失败，但本地显示已更新');
    }
  } catch (error) {
    console.error('切换模型可见性失败:', error);
    ElMessage.error('更新服务器失败，但本地显示已更新');
  }
}
```

#### 1.3 现有函数修改
修改 `updateModelVisibility` 函数，使其考虑单个模型的可见性设置：

```javascript
function updateModelVisibility(visible) {
  // 只对当前可见状态为true的模型应用全局设置
  Object.entries(loadedModels.value).forEach(([modelId, model]) => {
    const modelInfo = buildingModels.value.find(m => m.id === modelId);
    if (modelInfo && modelInfo.visible !== false) {
      model.visible = visible;
    }
  });
}
```

#### 1.4 CSS样式调整
无需额外CSS修改，现有的 `.model-actions` 样式已支持 `display: flex` 和 `gap: 0.5rem`，能够很好地容纳新按钮。

### 2. 后端API修改 (如果需要)

#### 2.1 模型更新接口 (`backend/app/main.py`)
确保PUT `/api/models/{model_id}` 接口支持更新 `visible` 字段：

```python
@app.route('/api/models/<model_id>', methods=['PUT'])
def update_model(model_id):
    try:
        data = request.get_json()
        # 更新模型的可见性和其他属性
        # 具体实现取决于现有的模型存储方式
        return jsonify({'success': True})
    except Exception as e:
        return jsonify({'success': False, 'error': str(e)}), 500
```

### 3. 功能特性

#### 3.1 按钮状态
- **显示状态**: 绿色按钮 (type="success") + 眼睛图标 (`el-icon-view`)
- **隐藏状态**: 橙色按钮 (type="warning") + 隐藏图标 (`el-icon-hide`)

#### 3.2 交互逻辑
- 点击按钮立即切换该模型在3D场景中的显示状态
- 状态同步到后端保存
- 提供用户友好的反馈消息
- 鼠标悬停显示tooltip说明

#### 3.3 与现有功能的兼容性
- 全局"显示模型"开关仍然有效，但只影响visible=true的模型
- 透明度和显示模式设置继续应用于可见的模型
- 不影响现有的编辑和删除功能

### 4. 预期效果

修改完成后，每个模型项将显示为：
```
[模型名称] [格式]    [👁️绿色] [✏️灰色] [🗑️红色]
```

用户可以：
1. 单独控制每个模型的显示/隐藏
2. 保持现有的全局控制功能
3. 通过颜色和图标直观了解每个模型的当前状态

## 修改文件列表
- `frontend/src/views/RtkMonitor.vue` (主要修改)
- `backend/app/main.py` (可能需要API完善)

## 风险评估
- **低风险**: 主要是添加功能，不影响现有代码逻辑
- **兼容性**: 与现有的全局控制功能完全兼容
- **用户体验**: 提供更精细的控制，提升用户体验

## 是否确认进行此修改？
请确认是否按照以上方案进行修改。如有调整需求，请告知具体要求。 